#include "main.h"
#include <stdio.h>

/**
 * mul - a function that multiplies two values
 * @a: first input
 * @b: second input
 * Return: multiplication result of the 2inputs
 * */

int mul(int a, int b)
{
	return (a * b);
}
