#include "main.h"
/**
 * add - a function that add two integers and return result
 * @one: first number
 * @two: second number
 * Return: one + two
 */
int add(int one, int two)
{
	return (one + two);
}
