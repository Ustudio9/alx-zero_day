#include "main.h"
/**
 * _abs - a function that computes the absolute value of an integer.
 * @n: Tested number
 * Return: 0 (Success)
 */

int _abs(int)
{
	n = n % 10;

	if (n < 0)
		n = -n;
	_putchar(n + '0');
	return (n);
}
