#include "main.h"

/**
 * _abs - a function that computes the absolute value of an integer.
 * @n: Tested number
 * Return: 0 (Success)
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (-0);
	}
	else
	{
		return (n);
	}

	return (0);
}
